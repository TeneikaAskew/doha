# Core dependencies with version pinning
# Using ~= for compatible release (allows patch updates, not minor/major)

# LLM API clients
anthropic~=0.76.0
pydantic~=2.12.0
loguru~=0.7.3

# PDF processing
PyMuPDF~=1.26.0

# RAG (recommended for precedent matching)
sentence-transformers~=5.2.0
numpy~=2.4.0

# Web scraping (for DOHA case scraping)
requests~=2.32.5
beautifulsoup4~=4.14.0

# Browser automation for DOHA scraping (recommended for bypassing bot protection)
# Install with: pip install playwright && playwright install chromium
playwright~=1.57.0

# Google Gemini support (optional - alternative LLM provider)
google-generativeai~=0.8.6

# Configuration management
python-dotenv~=1.2.0

# Data processing (inherited from parent project)
pandas~=3.0.0
pyarrow~=23.0.0
tqdm~=4.67.0
